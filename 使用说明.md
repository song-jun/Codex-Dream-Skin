# Codex Dream Skin 使用说明（Windows）

给 Codex 桌面端换一张会呼吸的脸：一张 16:9 纯背景图连续铺满整窗，侧栏 / 顶栏 / 输入框 / 建议卡都变成可读的半透明图层，原生控件全部可交互。本机 CDP 注入，**不改官方安装包、不改签名**。

> 非 OpenAI 官方产品。Codex 及相关权利归其权利人所有。

---

## 一、前置条件

1. **Codex 桌面端**：从 Microsoft Store 安装的 `OpenAI.Codex`（脚本会自动识别 Store 包，不要手动指定路径）。
2. **Node.js 22 或更高**：注入器需要它。装好后 `node -v` 能看到 ≥ v22 即可。
3. **网络**：首次安装不需要联网；换背景图时图片在本机。

> 不需要管理员权限。脚本只写 `%LOCALAPPDATA%\CodexDreamSkin` 和你的桌面/开始菜单快捷方式。

---

## 二、一键安装（推荐）

打开仓库的 `windows` 文件夹，**双击 `安装皮肤.bat`**。

脚本会自动完成：

1. 安装皮肤引擎到 `%LOCALAPPDATA%\CodexDreamSkin\engine`；
2. 在桌面和开始菜单创建 3 个快捷方式（启动 / 恢复 / 托盘）；
3. 启动系统托盘；
4. 用调试通道启动 Codex 并注入皮肤，最后自动验证。

看到 `[成功] 皮肤已启动` 就完成了，Codex 窗口会带着皮肤打开。

### 安装失败怎么办？

- **「Codex 仍开着 / 托盘还在运行」**：完全退出 Codex，再右键托盘图标 → 退出，然后再次双击 `安装皮肤.bat`。
- **「启动失败」**：看 `%LOCALAPPDATA%\CodexDreamSkin\injector-error.log`，通常是 Node 版本太低或 Codex 没装好。

---

## 三、一键卸载

双击 `windows\卸载皮肤.bat`。

脚本会：关闭 Codex → 移除皮肤与调试通道 → 恢复官方外观 → 删除快捷方式 → 重新打开官方 Codex。

> 如果提示「没有配置备份」（比如之前已经卸载过一次），脚本会自动改用「仅移除皮肤与快捷方式」的方式完成卸载，不会卡住。

卸载后 Codex 回到完全官方的外观，不会有任何残留注入。

---

## 四、日常使用：系统托盘

安装后右下角会有一个 Codex Dream Skin 托盘图标，右键即可：

| 菜单项 | 作用 |
|--------|------|
| **切换主题 / 已保存主题** | 在内置预设和你自己保存的主题间一键切换 |
| **更换背景图** | 导入一张 16:9 纯壁纸（≤16MB，≤16384px），自动适配焦点与配色 |
| **保存当前为主题** | 把当前「背景图 + 配色」存成一个可复用主题 |
| **暂停 / 恢复** | 临时关掉皮肤看官方效果，恢复时不用重装 |
| **完整恢复** | 等同于一键卸载 |

### 内置预设主题

- `preset-gothic-void-crusade` 哥特虚空远征（暗色，金色强调）
- `preset-arina-hashimoto` 桥本有菜（浅/暗皆可，默认背景）
- `preset-romantic-rose` 浪漫玫瑰
- `preset-ssyai-dream` 梦境紫粉

> 主题切换是实时的，不需要重启 Codex。

---

## 五、换背景图（重点）

皮肤的核心就是那一张铺满整窗的图。换图有两种方式：

**方式 A（推荐）：托盘 → 更换背景图**
选一张本机的 16:9 纯壁纸即可。脚本会校验尺寸和体积，自动生成焦点元数据并保存为主题。

**方式 B：命令行**
```powershell
powershell -NoProfile -ExecutionPolicy RemoteSigned -File "$env:LOCALAPPDATA\CodexDreamSkin\engine\scripts\switch-background.ps1" -ImagePath "D:\我的图.jpg"
```

### ⚠ 选图规则（很重要）

- **必须是纯背景**：不能带任何 Codex 界面（侧栏、卡片、输入框、logo、文字）。
- **16:9、≥2560×1440** 最佳；超宽图会自动进入「沉浸态」整窗铺满。
- 不要用 README 里的效果图当背景——那些是带 UI 的概念图，**只能看不能导入**。
- 想自己生成？参考 [`docs/reference-background-prompt-guide.md`](./docs/reference-background-prompt-guide.md) 的提示词模板。

---

## 六、桌面快捷方式说明

安装后桌面会有 3 个快捷方式：

| 快捷方式 | 作用 |
|----------|------|
| **Codex Dream Skin** | 启动带皮肤的 Codex（若 Codex 已开会先询问再重启） |
| **Codex Dream Skin - Restore** | 恢复官方外观并关闭调试通道 |
| **Codex Dream Skin - Tray** | 打开托盘控制面板 |

日常只需用「Codex Dream Skin」启动即可，它会自动带上皮肤。

---

## 七、高级 / 手动命令

一键 bat 本质上是调用 `windows\scripts` 下的 PowerShell 脚本，你也可以手动执行：

```powershell
# 安装（不创建快捷方式）
powershell -NoProfile -ExecutionPolicy RemoteSigned -File .\windows\scripts\install-dream-skin.ps1 -NoShortcuts

# 启动皮肤（若 Codex 已开，需加 -RestartExisting）
powershell -NoProfile -ExecutionPolicy RemoteSigned -File .\windows\scripts\start-dream-skin.ps1 -RestartExisting

# 独立验证 + 截图
powershell -NoProfile -ExecutionPolicy RemoteSigned -File .\windows\scripts\verify-dream-skin.ps1 -ScreenshotPath "$env:USERPROFILE\Desktop\check.png"

# 恢复官方外观
powershell -NoProfile -ExecutionPolicy RemoteSigned -File .\windows\scripts\restore-dream-skin.ps1 -RestoreBaseTheme

# 完整卸载（恢复 + 删快捷方式）
powershell -NoProfile -ExecutionPolicy RemoteSigned -File .\windows\scripts\restore-dream-skin.ps1 -RestoreBaseTheme -Uninstall
```

> 执行策略统一用 `RemoteSigned`，**不要用 Bypass**，也不要改系统的永久策略。

---

## 八、Codex 更新后怎么办？

Codex 自动更新后皮肤可能失效（新的 Store 包路径变了）。处理办法：**再次双击 `安装皮肤.bat`** 即可。脚本会自动发现新版本的 Store 包并重新注入，已保存的主题和背景图不会丢。

---

## 九、常见问题

**Q：双击 bat 后一闪而过？**
右键 → 用管理员身份运行没必要；多半是脚本里 `pause` 前报错了。把 bat 拖进 PowerShell 窗口运行，能看到完整报错。

**Q：皮肤装上了但 Codex 看起来没变？**
确认你启动的是「Codex Dream Skin」快捷方式（带调试通道），而不是直接从开始菜单开 Codex。直接开 Codex 不会带皮肤。

**Q：背景图没显示 / 还是旧图？**
托盘 → 切换到一个预设主题再切回来；或重新导入背景图。主题状态文件在 `%LOCALAPPDATA%\CodexDreamSkin\active-theme`。

**Q：怎么彻底关掉调试通道？**
不用皮肤时双击 `卸载皮肤.bat`，或托盘 → 完整恢复。这会关闭 9335 端口的本地调试监听。

**Q：安全吗？**
- 调试通道只绑 `127.0.0.1`（本机回环），不暴露到局域网。
- 不修改 `WindowsApps`、`app.asar` 或任何官方文件。
- 不改你的 API Key / Base URL。
- 皮肤运行期间不要跑来路不明的本机程序（Chromium CDP 无同用户鉴权）。

---

## 十、文件位置速查

| 内容 | 路径 |
|------|------|
| 皮肤引擎 / 运行时 | `%LOCALAPPDATA%\CodexDreamSkin\engine` |
| 当前主题 | `%LOCALAPPDATA%\CodexDreamSkin\active-theme` |
| 已保存主题 | `%LOCALAPPDATA%\CodexDreamSkin\themes` |
| 导入的背景图 | `%LOCALAPPDATA%\CodexDreamSkin\images` |
| 注入器日志 | `%LOCALAPPDATA%\CodexDreamSkin\injector.log` |
| 验证日志 | `%LOCALAPPDATA%\CodexDreamSkin\verify.log` |
| 主题 CSS（源码，带中文注释） | `windows\assets\dream-skin.css` |

---

## macOS 用户

macOS 的安装方式不同：打开 `macos` 文件夹，双击 `Install Codex Dream Skin.command` 即可。详见 [`macos/README.md`](./macos/README.md)。

---

皮肤有问题？先双击 `卸载皮肤.bat` 恢复官方外观，再重新安装。喜欢的话，给项目点个 Star。
